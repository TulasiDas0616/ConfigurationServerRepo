spring:
  datasource:
    url: jdbc:mysql://localhost:3306/department_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    eureka:
  instance:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/

  

